{
  "name": "skyroute",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "dev:client": "vite",
    "dev:server": "node --watch server/index.mjs",
    "start": "node server/index.mjs",
    "test": "node tests/flight-regression.mjs && node --test tests/phase2.test.mjs tests/plateau.test.mjs",
    "test:browser": "playwright test",
    "capture:fixture": "node scripts/capture-fixture.mjs",
    "build:plateau": "node scripts/build-plateau-haneda.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "saxes": "^6.0.0",
    "typescript": "^5.7.3",
    "vite": "^6.2.0"
  },
  "engines": {
    "node": ">=22.6"
  }
}
